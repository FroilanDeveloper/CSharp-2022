function addition (a,b) {
  const sum = 1000 + 10;
  return sum // didnt know i had to put a return here
}
console.log(addition());// and i thought we console log addition(a,b) but its working with just an empty ()